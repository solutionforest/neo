name: neo-builder
domain: none
port: 9100
https: false
server: "flint-153"

env:
  NEO_SOURCE_DIR: /src/neo
  NEO_OUTPUT_DIR: /output

volumes:
  binaries:
    path: /output
    mount: /srv/neo-binaries
